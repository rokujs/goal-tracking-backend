const { Router } = require('express')
const router = Router()

// const {  } = require('../controllers/histories.controller')

// Single goal history
router.get('/history/:id')

// Add Goal
router.post('/goals/add')

module.exports = router
